### css阻塞渲染的资源   
利用媒体类型和渲染来解除对渲染的阻塞。  

#### 阻塞渲染
关键渲染路径要求我们同时具有 DOM 和 CSSOM 才能构建渲染树。这会给性能造成严重影响：HTML 和 CSS 都是阻塞渲染的资源。 HTML 显然是必需的，因为如果没有 DOM，我们就没有可渲染的内容，但 CSS 的必要性可能就不太明显。  
当css被阻塞渲染的时候，会造成内容样式短暂失效  

#### 利用媒体查询
利用浏览器执行的媒体查询，符合条件的时候，浏览器将会阻塞渲染，直到样式表下载完毕。  




